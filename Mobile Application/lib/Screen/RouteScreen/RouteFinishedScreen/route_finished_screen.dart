import 'package:fyp_app/Screen/screen_export.dart';

class RouteFinishScreen extends StatelessWidget {
  const RouteFinishScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Finish'),),
    );
  }
}
